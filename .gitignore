node_modules/
out/
dist/
.DS_Store
*.log
.env
.env.*

# Screenshots of the journal surface show real entries — six months of personal
# reflection. Agent runs drive that surface against a copy of the live corpus, so
# these must never be committed, private repo or not.
.progress/journal-surface/
.progress/**/journal*.png
