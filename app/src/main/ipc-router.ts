import type { IpcMain } from 'electron'

/**
 * STUB — replaced by the storage workstream. Registers a handler for every entry in
 * IPC_CHANNELS, backed by the file repositories.
 */
export function registerIpcHandlers(_ipcMain: IpcMain): void {
  // intentionally empty until the repositories land
}
